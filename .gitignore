# dependencies
/node_modules

# next.js
/.next/
/out/

# misc
.DS_Store
*.pem
npm-debug.log*

# local database — never commit real data. The glob covers the throwaway
# databases a migration or build check creates alongside dev.db.
prisma/*.db
prisma/*.db-journal

# local env — real password lives here, never committed
.env

# typescript
*.tsbuildinfo
next-env.d.ts
